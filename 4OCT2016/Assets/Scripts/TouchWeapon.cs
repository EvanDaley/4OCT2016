using UnityEngine;
using System.Collections;

public class TouchWeapon : MonoBehaviour {

	void Update ()
	{
		if(Input.touchCount > 0 || Input.GetMouseButton(0))
		{
			//the user touched the screen, get position

		}
	}
}
